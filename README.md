# command

Commands are objects that wrap a method to help update the UI safely. They act as a state machine for asynchronous actions, tracking whether a task is currently executing, has finished successfully, or has failed.